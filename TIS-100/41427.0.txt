@0


@1
MOV UP,ACC
MOV ACC,DOWN
MOV ACC,RIGHT

@2
 MOV 0,RIGHT
 MOV -999,RIGHT
NEXT: MOV LEFT,ACC
 JEZ SENDA
 SAV
 MOV 1,RIGHT
 SUB RIGHT
 JLZ NEXT
 SWP
 MOV 0,RIGHT
 MOV ACC,RIGHT
 JMP NEXT
SENDA: MOV 1,RIGHT
 MOV RIGHT,DOWN

@3
#ONE VALUE MEMORY
MEMLOOP:
  MOV ANY,ACC
  JEZ WRITE
READ:
  SWP
  MOV ACC,LAST
  SWP
  JMP MEMLOOP
WRITE:
  MOV LAST,ACC
  SAV

@4
#ONE VALUE MEMORY
MEMLOOP:
  MOV ANY,ACC
  JEZ WRITE
READ:
  SWP
  MOV ACC,LAST
  SWP
  JMP MEMLOOP
WRITE:
  MOV LAST,ACC
  SAV

@5
 MOV 0,LEFT
 MOV 999,LEFT
NEXT: MOV UP,ACC
 JEZ SENDI
 SAV
 MOV 1,LEFT
 SUB LEFT
 JGZ NEXT
 SWP
 MOV 0,LEFT
 MOV ACC,LEFT
 JMP NEXT
SENDI: MOV 1,LEFT
 MOV LEFT,DOWN

@6
MOV UP,DOWN

@7


@8
MOV UP,DOWN

@9
MOV UP,DOWN

@10

